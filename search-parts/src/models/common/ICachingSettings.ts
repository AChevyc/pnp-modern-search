export interface ICachingSettings {

    /**
     * Flag indicating if the cache should be enable
     */
    enableCaching: boolean;

    /**
     * Cache duration in minutes
     */
    cacheDuration: number;
}